apiVersion: v1
kind: Service
metadata:
  name: {{ include "tomcat1.fullname" . }}
  labels:
    {{- include "tomcat1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tomcat1.selectorLabels" . | nindent 4 }}
